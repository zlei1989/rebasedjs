/**
 * SPA 入口占位。
 *
 * Task 1 阶段 apps/web-koa 的依赖清单不含 react/react-dom（Plan 2b Task 9 才加入），
 * 故此处不 import react 也不书写 JSX，否则本包 tsc --noEmit 无法通过。
 * Task 9 挂载真实页面（与 web-next 共享同一套 ui/client）时替换本占位。
 */
